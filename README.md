# SC SPAM

$ apt update && apt upgrade
$ pkg install python
$ pkg install bash
$ pkg install git
$ git clone https://github.com/Sxp-ID/Kang-Spam
$ cd Kang-Spam
$ git pull
$ bash install.sh

SUMBER SC:GK TAU LUPA
